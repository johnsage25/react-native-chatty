/**
 * If the skeleton loader is enabled, then render the skeleton loader. Otherwise, render the children
 * @param {any} props - any
 * @returns A skeleton component / Native view object
 */
export declare function Skeleton(props: any): JSX.Element;
